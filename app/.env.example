# ==== 부서장 페르소나 에이전트 환경변수 ====

# 데이터베이스 파일 위치 (Docker 볼륨: /app/data)
DATA_DIR=./data
DATABASE_URL=./data/dept.db

# === RAGFlow (외부 서버) ===
# 관리자: admin@shinhanlife.co.kr (키는 보안 관리)
RAGFLOW_BASE_URL=https://ragflow.ax-demo.com
RAGFLOW_API_KEY=

# RAG 검색 옵션
RAG_TOP_K=5
RAG_SIMILARITY_THRESHOLD=0.2

# === LLM (LiteLLM 게이트웨이 → DeepSeek) ===
LLM_BASE_URL=https://litellm.ax-demo.com/v1
LLM_API_KEY=
LLM_MODEL=deepseek-v4-flash

# === 인증 ===
# 허용 이메일 도메인 (쉼표 구분)
ALLOWED_EMAIL_DOMAINS=shinhan.com,shinhanlife.co.kr
SESSION_COOKIE_NAME=dept_session
# 세션 만료(ms, 기본 7일)
SESSION_TTL_MS=604800000

# HTTPS 앞단(프록시)에서만 Secure 쿠키
COOKIE_SECURE=false
