[project]
name = "meta-prompt-refiner"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
meta-prompt-refiner = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meta_prompt_refiner"]
