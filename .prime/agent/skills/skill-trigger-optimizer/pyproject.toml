[project]
name = "skill-trigger-optimizer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
skill-trigger-optimizer = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/skill_trigger_optimizer"]
